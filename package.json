{
  "name": "scrap-azure-function",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "azure:remote-deploy": "func azure functionapp publish scrapazurefunction --build remote",
    "azure:login": "az login"
  },
  "devDependencies": {
    "@azure/functions": "^1.2.3",
    "typescript": "^4.0.0"
  },
  "dependencies": {
    "puppeteer": "^12.0.1"
  }
}
